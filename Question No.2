#Question No.2
#Test script to check whether the json we got from api is in range or not

import pytest
import requests

#case1 where response time is 0.145 seconds and it is within the range (range = 0.0 to 1.0)
def test_method1():
    min_response_time = 0.0
    max_response_time = 1.0
    response = requests.get("https://reqres.in/api/users?page=2")
    time = response.elapsed.total_seconds()
    print(time)
    assert min_response_time <= time <= max_response_time


#case2 where response time is 0.110 seconds. But time is more than the range we took ie (range = 0.0 to 0.1)
def test_method2():
    min_response2 = 0.0
    max_response2 = 0.1
    response2 = requests.get("https://reqres.in/api/users/23")
    time2 = response2.elapsed.total_seconds()
    print(time2)
    assert min_response2 <= time2 <= max_response2


#case3 where response time is 0.0668 seconds and time elapsed is within range i.e (range = 0.0 to 1.0)
def test_method3():
    min_response3 = 0.0
    max_response3 = 1.0
    response3 = requests.get("https://reqres.in/api/unknown")
    time3 = response3.elapsed.total_seconds()
    print(time3)
    assert min_response3 <= time3 <= max_response3


#case4 where response time is way more than within the specified range i.e (range = 0.0 to 1.0)
def test_method4():
    min_response4 = 0.0
    max_response4 = 1.0
    response4 = requests.get("https://reqres.in/api/users?delay=3")
    time4 = response4.elapsed.total_seconds()
    print(time4)
    assert min_response4 <= time4 <= max_response4
